version: '3'

services:
  flask_app:
    image: python:3.11-slim
    container_name: jenkins_fitness_app
    working_dir: /app
    volumes:
      - ./:/app
    ports:
      - "5001:5000"
    command: >
      sh -c "pip install -r requirements.txt &&
             python app.py"